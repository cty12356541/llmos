# 议题 31：重复建设评估与继续投入边界

> 状态：评估结论已定；阶段 B 实施顺序调整（Go/C# 后移、B-TASK 前移）已于 2026-08-04 经[阶段 B 权威进度单](../management/stage-b-progress.md)采纳生效
>
> 增补评估：[议题 32：核心设计理念撞车风险评估](./32-核心设计理念撞车风险评估.md)（2026-08-04）——以系统完备性判据重评后，先例表由 7 项扩充至 20+ 项，P1 独创性表述和独有集收敛以议题 32 第 7 节为准
>
> 日期：2026-08-03
>
> 适用基线：[架构设计总纲 v0.5](../design/06-架构设计总纲-v0.5.md)、[阶段 B 技术选型](../management/stage-b-technology-selection.md)、[阶段 B 权威进度单](../management/stage-b-progress.md)
>
> 议题范围：判断 NLOS 是否正在重复建设已有 Agent OS/runtime，明确必须自持、应当复用和暂缓建设的边界，并为继续投入设置可证伪的验收门。本议题不把外部项目说明当作实现证据，也不自动改变阶段 B 当前工作项顺序。

## 1. 问题与结论

问题不是“是否使用了别人也使用的 Process、Capability、WAL 或 Fiber”，而是：

1. NLOS 的核心哲学是否已有公开先例；
2. 哪些机制构成 NLOS 的系统身份，必须由项目掌握；
3. 哪些只是成熟基础设施，继续自研会扩大维护面；
4. 项目在什么证据出现后才值得继续扩大到桌面、分布式和生态。

结论：

> **NLOS 存在重复建设风险，但仍值得有条件地继续。值得继续的是“通用 AI 原生 Application 的提交、副作用、资源与控制权威层”，不值得继续的是横向重建另一个包办 Agent、RPC、沙箱、工作流、桌面和分布式基础设施的 Agent OS。**

具体判断：

- “概率模型位于不可信用户态，确定性内核在不可绕过边界强制权限、资源和副作用”已有多个公开先例，不能作为独占创新或“世界首创”声明；
- Capability、可消耗 Budget、Checkpoint/Replay、Agent Process、Context paging、调度、Wasm/V8 隔离和 durable workflow 均有成熟机制或近邻项目，默认应复用、适配或兼容；
- `TaskAttempt → CommitPermit → LogicalEffect/EffectPermit → Operation → Receipt → reconcile` 的组合，以及它与 pure-code/LLM Application、统一 ControlCommand、多维 Resource Manager 的衔接，仍是项目应重点验证的差异；
- 差异目前主要处于 `DESIGN`，只有完成最小 Slice K 和故障矩阵后，才能晋升为已实现能力；
- 若该纵切面的执行底座可由现有 durable execution 引擎加薄适配满足，项目应复用该底座并把自研范围收敛到 NLOS authority contract；这只收缩实现面，不自动收缩完整 NLOS 的对象模型、系统范围和最终产品目标。

### 1.1 原始目标：迁移现代计算体系，而不是只造 Agent OS

项目的原始方法论是：

> **系统性审计和迁移现代计算体系中与概率智能工作负载相关的成熟机制：直接可继承者复用其不变量，需要语义适配者建立 NLOS 契约，存在不可消除语义断裂者重新设计，并以 conformance、故障注入和纵向应用证据验证。**

更简洁地说：

> **把现代计算的成熟机制迁移到概率智能系统，在语义断裂处重新发明。**

这里的“完整复现”不是重新手写数据库、executor、Wasm VM、网络栈、密码学和窗口系统，而是系统覆盖计算机发展中已经证明有效的理论、控制回路、不变量和失败语义。NLOS 应复现计算机文明积累的控制智慧，不复现已经解决的底层工程。

因此本项目不能被缩减为：

- 一个安全 tool wrapper；
- 一个多 Agent workflow；
- 一个 LLM memory runtime；
- 一个自然语言桌面壳；
- 一个 durable execution adapter。

这些都可以成为 NLOS 的组件或宿主，但不能单独替代其通用 Application、系统权威、现代资源、分布式、桌面和生态终局。

### 1.2 三类迁移

| 类别 | 判定 | 典型机制 | 实施要求 |
|---|---|---|---|
| A：直接迁移 | 不变量和失败语义基本不变，只更换被管理对象 | 有界队列、backpressure、WAL、MVCC、CAS、lease/fence、Capability、Namespace、structured concurrency、process isolation、outbox、idempotency、package signature | 复用成熟实现，增加 NLOS adapter、binding Receipt 和 conformance；不得因改名重复发明 |
| B：语义适配 | 控制机制仍成立，但资源、状态、计量或恢复对象发生变化 | Context residency、token/model slot、KV/prefix cache、Agent checkpoint、human attention、LLM/tool deadline、多维 Resource Manager | 由 NLOS 定义公开契约和差异化失败语义，底层继续优先组合成熟机制 |
| C：重新设计 | 传统计算没有完整对应物，或直接迁移会破坏权限、语义或副作用不变量 | typed Intent、随机 TaskAttempt、CommitPermit、LogicalEffect/EffectPermit、effect unknown、semantic identity/provenance、Authority-aware View、模型/人工验证边界 | 必须记录最近现代机制、语义断裂、不能无损组合的原因，并给出可证伪实验 |

迁移的对象是状态机、控制回路、隔离边界和失败语义，不是术语。把 Agent 命名成 Process 不会自动获得进程隔离，把 Context 命名成虚拟内存也不会自动获得无损换页。

### 1.3 从计算机发展阶段继承什么

NLOS 不机械重演旧系统，而是抽取各阶段留下的长期机制：

| 计算发展阶段 | 应迁移的长期成果 | NLOS 对应方向 |
|---|---|---|
| 批处理 | Job、队列、依赖、完成回执、失败重跑 | Task/TaskPlan、Receipt、durable execution |
| 多道程序与分时 | 隔离、抢占、公平、优先级、交互前台 | Process/Fiber/Activation、SchedulerDomain、QoS |
| Unix 与 capability 系统 | 进程、组合通信、namespace、最小权力、机制/策略分离 | Process、Channel、Namespace、Capability、微内核/系统服务边界 |
| 数据库与事务系统 | WAL、MVCC、CAS、snapshot、唯一提交、recovery | durable authority、TaskSnapshot、TaskHead/CommitPermit、Outbox |
| 分布式系统 | lease、epoch、fencing、quorum、reconcile、故障域 | Cell/Authority、generation、Quota/Capacity Lease、effect reconciliation |
| 云原生与现代资源管理 | controller、声明式状态、cgroup、bulkhead、pressure/reclaim | ResourceController/Group、AdmissionDecision、Global/Cell/Worker |
| 软件平台与现代桌面 | Application/Package/ABI、安装更新、权限 UI、Task Manager | Application/Package/KABI/SABI、可信 UI、统一 ControlCommand |
| 概率智能时代 | 随机计划、语义状态、模型不确定性、真实副作用、人类注意力 | Intent、TaskAttempt、EffectPermit、SemanticEvent、Receipt、attention resource |

总原则是：

> **继承历史成果，不继承历史包袱；迁移系统不变量，不复制旧实现形式。**

### 1.4 完整目标与当前窄切片的关系

最小 Slice K 不是把项目终局收缩成副作用引擎，而是第一条构造性验证：它要证明同一迁移方法能够把 Application、Task、Process/Fiber、Operation、Artifact/Receipt、权限、资源、控制和恢复组合成真实系统路径。

```text
现代计算历史与成熟机制
        ↓
机制迁移与语义断裂分析
        ↓
NLOS 确定性权威层与可替换系统服务
        ↓
AI 原生通用 Application 平台
        ↓
单机、分布式、自然语言桌面与第三方生态
```

采用 Tokio、SQLite、Wasmtime、Temporal/Restate 或未来其他成熟引擎，只表示 NLOS 复用了 A/B 类机制的实现底座。系统身份来自完整对象模型、权威边界、兼容契约、失败语义、用户控制和应用体验，而不来自“每一行底层代码都由项目自己编写”。

## 2. 公开先例与重叠范围

> 查询日期：2026-08-03。下表只记录公开资料所声明的范围，不替代源码审计、复现实验、许可证审查或专利检索。

| 公开项目/工作 | 已公开的相近思想 | 与 NLOS 的主要差异 | 来源 |
|---|---|---|---|
| AIOS | Agent OS、kernel/SDK、LLM/Context/Memory/Storage/Tool 调度与管理 | 倾向把 LLM 嵌入 OS/kernel；NLOS 明确把模型置于不可信用户态 | [AIOS](https://github.com/agiresearch/AIOS)、[论文](https://arxiv.org/abs/2403.16971) |
| Agent OS Kernel | `USER SPACE (Untrusted LLM)`、确定性 kernel enforcement、policy/signal/audit | 主要是安全治理内核，不是完整通用 Application/Package/Artifact/桌面模型 | [PyPI 项目说明](https://pypi.org/project/agent_os_kernel/1.1.0/) |
| Castor | 不可信 LLM 用户态、确定性微内核、syscall proxy、Capability Budget、原子父子委托、Checkpoint/Replay、HITL、Context MMU、SQLite | 当前公开模型以 Agent/tool syscall 为中心；未给出 NLOS 规划的跨 Attempt 唯一提交和逐 LogicalEffect 权威链 | [Castor 白皮书](https://substratumlabs.ai/castor-docs/docs/whitepaper/) |
| Agent libOS | AgentProcess、身份/lineage/lifecycle、typed object memory、Capability、human queue、checkpoint、event/audit、异步调度 | library-OS runtime substrate；范围窄于 NLOS 的通用应用、资源控制、统一桌面和分布式路线 | [Agent libOS](https://arxiv.org/abs/2606.03895) |
| AgentRM | OS 风格 Agent scheduler、zombie reaping、rate-limit admission、Context 生命周期和分层驻留 | 聚焦调度与 Context resource manager，不提供完整副作用/提交权威 | [AgentRM](https://arxiv.org/abs/2603.13110) |
| Rivet agentOS | V8/Wasm、虚拟文件/进程/网络、deny-by-default 权限、资源限制、durable workflow | 重点是可移植 Agent 虚拟运行环境；NLOS 目标包含宿主 Process、第三方通用 Application 和系统级 Receipt | [Rivet agentOS](https://github.com/rivet-dev/agentos) |
| Temporal/Restate/Dapr 等 durable execution | event history、durable timer、retry、workflow/activity、恢复和幂等模式 | 通用执行基础设施；NLOS 仍需定义 Agent/Application 身份、Capability、EffectPermit、Resource 和用户控制契约 | [Temporal](https://docs.temporal.io/)、[Restate](https://docs.restate.dev/)、[Dapr Workflow](https://docs.dapr.io/developing-applications/building-blocks/workflow/workflow-overview/) |

由此可以确认：

```text
不可信 LLM 用户态
+ 确定性微内核
+ capability/budget
+ checkpoint/replay
+ context paging
+ Agent 调度
```

这一组合本身已经有强近邻。NLOS 不得仅靠更换术语或扩大功能清单声称架构独创。

## 3. NLOS 必须自持的系统语义

“自持”不要求从零实现所有底层算法，而是要求 NLOS 拥有公开契约、权威状态、失败语义和 conformance；底层可以由成熟组件承载。

### 3.1 TaskAttempt 与唯一 canonical commit

NLOS 必须证明：

- 同一 Task 可以安全运行 retry、hedge 或候选 Attempt；
- 每个 Attempt 具有独立 Snapshot、generation、资源与取消域；
- 只有一个 Attempt 获得 CommitPermit 并推进 TaskHead；
- losing/cancelled/stale Attempt 不能污染 canonical Artifact 或状态；
- “cancel 先于 permit”和“permit 先于 cancel”只能产生规范允许的线性化结果。

这不是普通 workflow retry 的别名，而是海量 Agent 并发、候选执行和唯一结果提交的共同权威。

### 3.2 LogicalEffect、EffectPermit 与外部未知状态

外部系统可能已经接受邮件、付款、文件写入或设备命令，而 NLOS 在记录结果前崩溃。该裂缝不能被简单改名为 `FAILED`，也不能只靠 replay 消除。

NLOS 必须自持：

```text
LogicalEffect identity
  → per-slot EffectPermit
  → Operation generation / cancel epoch
  → provider idempotency or explicit GuaranteeTier
  → durable Receipt / effect history
  → NO_EFFECT | COMMITTED | PARTIAL | UNKNOWN
  → reconcile / compensation / human decision
```

当前 Operation、callback fence、durable authority 和 Outbox 已提供局部地基，但 `EffectPermit`、跨 Attempt effect history、permit adoption/quarantine 尚未实现，不得提前宣称该差异已经兑现。

### 3.3 多维 Resource 权威

NLOS 应保持以下分离：

- `ResourceAccount`：费用、token、可转移信用和账本守恒；
- `ResourceGroup`：CPU、memory、I/O、network、accelerator 等宿主容量与压力隔离；
- `ResourceController`：真实强制器及其 `HARD_ENFORCED / METERED / COOPERATIVE / UNSUPPORTED` 能力；
- `AdmissionPlan/Decision`：dispatch 前的多资源 prepare/commit/abort 与恢复。

不能用余额冒充物理隔离，也不能用 scheduler counter 直接改写信用账本。实现应先选最小资源集合验证，不为完整分布式资源平面预付复杂度。

### 3.4 通用 Application 与统一控制路径

NLOS 的验收对象不能只有 Agent：

- 同一系统必须运行 LLM Process、pure-code Process 和无 Task/IntentSpec 的后台 System Process；
- Application、Package、Installation、Executable、Process、AgentRole、Task、Artifact 必须保持身份分离；
- NL、GUI、CLI、API 必须收敛到同一个 typed Intent/ControlCommand、Capability、Resource 和 Receipt 路径；
- 自然语言是首要界面，但不是权限后门或唯一机器接口。

这些要求把 NLOS 从安全 Agent wrapper 推向通用 AI 原生应用平台，也是 Slice K 必须保留的边界。

## 4. Build / Reuse / Avoid 边界

| 类别 | 当前边界 | 原因 |
|---|---|---|
| **NLOS 必须拥有契约和权威** | nominal ID/generation、TaskAttempt/TaskSnapshot、CommitPermit、LogicalEffect/EffectPermit、Operation/Receipt、unknown-effect reconcile、ResourceAccount/Group 语义、ControlCommand、Artifact ownership | 这些定义 NLOS 的系统身份和失败语义 |
| **复用成熟实现并包适配层** | Tokio、SQLite、Wasmtime/WASI、Protobuf/Buf、成熟 CBOR/crypto、宿主 Process/Job/cgroup/sandbox、Tauri、OpenTelemetry、内容寻址与文件系统原语 | 避免重建 executor、数据库、虚拟机、编码器、密码学、窗口壳和观测后端 |
| **优先兼容或对照现有系统** | Temporal/Restate/Dapr durable execution、gRPC/Connect/现有本地 RPC、OS 服务发现、现有 package/OCI/WIT | 先证明现有系统无法无损承载 NLOS 不变量，再扩大自研表面 |
| **Slice K 前暂缓** | 完整桌面、应用商店、分布式 consensus/数据库、跨节点迁移、完整语义结算、海量语言 SDK、通用插件市场 | 防止横向建设掩盖核心纵切面未成立 |
| **禁止默认自研** | 通用 async executor、数据库/WAL、Wasm VM、通用 CBOR/Protobuf、密码算法、通用 RPC/服务发现平台 | 安全和维护成本高，且不能直接形成 NLOS 差异 |

约束：如果成熟组件缺少 NLOS 所需语义，应优先增加 facade、adapter、sidecar、conformance 和故障测试；只有证据证明组合无法满足不变量时，才新建底层实现。

## 5. 对阶段 B 当前进展的诊断

阶段 B 采用 Tokio、SQLite、Protobuf/Buf 和候选 Wasmtime/Tauri，说明技术路线并非默认从零重写底层。当前 Operation、Outbox、fault injection、cross-language IPC 也产生了真实局部 Evidence。

风险在于 `B-SCHEMA` 已连续扩展 framing、Unix socket、Windows named pipe、ServiceDirectory、三语言 client、协商、重连、幂等、deadline 和 OperationControl，而最能区分 NLOS 的 `B-TASK`、`EffectPermit` 和 `B-SLICE-K` 尚未开始。若继续横向扩张通用 RPC、服务发现和语言 SDK，项目会在证明存在价值前先承担平台维护成本。

因此形成以下边界结论：

1. Rust/TypeScript/Python 已足以证明当前跨语言协议可行；
2. Go/C# generation/golden 与独立 IPC PoC 仍有冻结前价值，但“增加第四种语言”不能证明 NLOS 核心成立；
3. Go/C# 不应推动 SABI 在 Task/Effect/Receipt 语义稳定前过早冻结；
4. 是否把 Go/C# PoC 后移到最小 Task/Effect 纵切面之后，属于阶段 B 权威进度单的顺序变更——该变更已于 2026-08-04 经[阶段 B 权威进度单](../management/stage-b-progress.md)采纳生效（Go/C# 探针 DEFERRED，`B-TASK` 升为唯一主线）；
5. `nlos-ipc` 应保持 NLOS 控制语义的薄承载层，不发展为通用 gRPC、服务网格或跨网络服务发现替代品。

## 6. 继续投入的最小证据门

在扩大桌面、分布式、生态和更多 SDK 前，Slice K 至少必须完成以下可复现路径：

1. 创建一个真实用户 Task 和冻结输入的 TaskSnapshot；
2. 同时启动两个竞争同一 TaskHead 的 TaskAttempt；
3. 只有一个 Attempt 获得 CommitPermit；
4. 两个 Attempt 请求同一个 LogicalEffect，只有合法 EffectPermit 持有者可 dispatch；
5. 在外部调用前、调用中、调用成功后写 Receipt 前分别强制崩溃；
6. 重启后区分 `NO_EFFECT / COMMITTED / PARTIAL / UNKNOWN`，未知状态进入 reconcile，不冒充失败；
7. 迟到 callback、旧 generation 和 cancelled Attempt 不能污染新执行；
8. 形成可查询的 Artifact、nested Receipt 和 TaskCommitReceipt；
9. CLI 与至少另一个入口产生等价 ControlCommand/Receipt；
10. 同时运行一个 LLM Process、一个 pure-code Process 和一个非 Task 后台 System Process；
11. 用相同故障场景对照至少一个成熟 durable execution 方案，记录哪些 NLOS 不变量可直接复用、哪些需要 authority extension；
12. 所有结论按 Evidence profile 限定，不把单机 PoC 外推为生产或分布式保证。

### 6.1 继续、收缩与转向条件

```text
最小 Slice K 证明独立权威语义
  → 继续扩大 Process / Resource / Control / SDK / Desktop

成熟 durable engine + 薄 adapter 可满足执行底座不变量
  → 复用执行底座，收缩自研实现面；完整 NLOS 系统范围保持不变

无法证明 unique commit / effect fence / recovery
  → 停止横向扩张，修正或否决当前核心架构
```

这是一道可证伪的投资门，不是预设项目必然成功。

## 7. 对外定位与声明边界

当前不应使用：

- “首次把 LLM 当作不可信用户态”；
- “首个 Agent 微内核”；
- “首个 capability/budget Agent OS”；
- “已经具备 Windows/macOS 级系统能力”；
- “已有完整副作用 exactly-once 保证”。

更准确的候选定位是：

> **面向通用 AI 原生 Application 的系统权威层：在并发 Attempt、真实外部副作用、多维资源和多入口用户控制之间提供可持久、可恢复、可审计的统一语义。**

该定位仍是目标声明。只有 Slice K 的实现、故障证据和竞品对照完成后，才能晋升为能力声明。

## 8. 与当前规范和管理对象的关系

- 本议题不修改 v0.5 的最高目标、对象模型或 Slice K Requirement；它为“为什么必须纵切面优先、为什么不能横向重建基础设施”补充 prior-art 与 build/reuse rationale；
- 本议题确认 v0.5 的 `PHIL-TRANSFER-001/002`、`PHIL-MATRIX-001/002` 仍是正确约束：先迁移成熟机制，只有语义断裂才新增 NLOS 抽象；
- 复用现有 runtime、durable engine、数据库、沙箱或桌面壳，只改变 build/reuse 决定，不得被解释为把最高目标永久收缩为 middleware、插件或 authority sidecar；
- 阶段 B 的实时实现状态和下一验收门仍以[权威进度单](../management/stage-b-progress.md)为准；
- "Go/C# 后移、B-TASK/EffectPermit 前移"已于 2026-08-04 采纳，阶段 B 进度单与语言支持计划已在同一 canonical commit 中同步；
- 若后续发现更接近先例或反例，应追加来源、降低原创性/能力 Claim，而不是删除本议题中的负面结论。
