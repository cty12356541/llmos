[package]
name = "nlos-identity"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
ed25519-dalek.workspace = true
nlos-clock = { path = "../nlos-clock" }
nlos-types = { path = "../nlos-types" }
rusqlite.workspace = true
sha2.workspace = true

[dev-dependencies]
ed25519-dalek = { workspace = true, features = ["rand_core"] }
# Test-only fault-injection VFS shim for the rotation kill-window matrix
# (tests/identity_rotation_fault_injection.rs); never a production dependency.
nlos-store-fault = { path = "../nlos-store-fault" }

[lints]
workspace = true
